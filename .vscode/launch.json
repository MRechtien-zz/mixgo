{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch MixGo",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}",
            "args": ["${workspaceFolder}/config/config-xr-local.yml"],
            "cwd": "${workspaceFolder}"
        }
    ],
    "compounds": []
}
